// import React from 'react'
// import { expect } from 'Chai'
// // import { spy } from 'Sinon'
// import { configure, shallow } from 'enzyme'

// // import {  } from 'enzyme';
// import Adapter from 'enzyme-adapter-react-16';

// configure({ adapter: new Adapter() });

// import {
//   LineChartGraph,
//   BarChartGraph,
//   AreaChartGraph,
//   RadarChartGraph,
//   ScatterChartGraph,
//   PieChartGraph
// } from './graphs'
// import SingleGraphView from './SingleGraphView';

// describe('SingleGraphView component', () => {
//   let group;

//   beforeEach('create stuff', () => {
//     group = shallow(<SingleGraphView />)
//   });

//   it('Has LineChartGraph, BarChartGraph, AreaChartGraph, RadarChartGraph, ScatterCHartGraph, PieChartGraph', () => {
//     expect(group.find(LineChartGraph).length).to.be.equal(1);
//     expect(group.find(BarChartGraph).length).to.be.equal(1);
//     expect(group.find(AreaChartGraph).length).to.be.equal(1);
//     expect(group.find(RadarChartGraph).length).to.be.equal(1);
//     expect(group.find(ScatterChartGraph).length).to.be.equal(1);
//     expect(group.find(PieChartGraph).length).to.be.equal(1);
//   })
// })
